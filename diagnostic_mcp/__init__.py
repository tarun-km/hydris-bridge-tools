"""Hydris Pulse Diagnostic MCP server, Bridge track Work Item 2.

Builds on the Work Item 1 skeleton (`hydris-mcp-integration`): the same
two-identity auth model, per-call authorization pipeline, and fail-closed
audit sink, extended with tools 1 to 8 (`list_factories` through
`get_compliance_status`) in LLM-oriented, BRIDGE-3/BRIDGE-4 compliant
form. Destined for `lite/diagnostic_mcp/` in the Lite monorepo, same as
Work Item 1 - see docs/scope.md.
"""

__version__ = "0.2.0"
